version: '3.8'

services:
  loadbalancerService:
    image:  nginx
    container_name: LoadBalancer

  backendService:
    image: php-fpm
    container_name: BackendPhp

  databaseService:
    image: mariadb
    container_name: Database